多实例部署的时候，需要把现在的协作改为redis，现在是内存里面管理协作者

模型训练运行时：当前先采用 `model_training_runtime` 单服务 + 服务内受控
subprocess 的方式快速验证训练契约、数据物化、事件和产物校验。待直接执行链路
稳定后，再评估是否需要把任意用户代码迁移到单任务容器或 Job，以加强文件系统、
网络和 GPU 隔离；容器不是当前阶段的前置条件。
