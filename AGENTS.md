# 项目开发规范（AGENTS.md）

适用范围：`frontend_v2`、`automl_server`、`model_trainer`、`model_deploy`、`auto_augment_pipeline` 及相关脚本。

---

## 一、总体原则

- 优先保证代码可读性、可维护性，避免炫技写法。
- 优先做直接、可落地的实现，避免过度设计。
- 修改前先理解现有上下文，不凭印象重写已有逻辑。
- 不要为了“理论上更优”破坏当前项目已经形成的实现习惯。
- 一个改动只解决当前问题，非必要不要顺手扩散重构。
- 写代码前先用自然语言说明实现思路。
- 改完必须自行验证；前端至少执行构建检查，后端至少做静态检查或最小链路验证。

---

## 二、产品与交互原则

- 页面 UI 要尽量简约、克制、清晰，让用户一眼知道当前能做什么。
- 不要在页面上堆冗余解释性文本；能靠布局、标题、按钮文案表达清楚的，不再额外写说明段落。
- 信息展示优先级要明确：
  - 先展示用户当前最关心的核心操作
  - 再展示必要状态
  - 最后才是辅助信息
- 避免把多个复杂场景混进同一个页面。
- 如果一个场景已经明显不同于现有工作流，应优先做独立页面或独立工作台，不要强行复用旧页面造成冲突。
- 交互应偏高效：
  - 常用操作要尽量少点击
  - 支持分页、快捷切换、必要的键盘操作
  - 减少用户在多个区域之间来回跳转

---

## 三、代码风格与实现要求

- 默认使用简单、直白的实现方式。
- 避免复杂正则表达式处理业务逻辑，除非明确必要。
- 处理文本时优先顺序：
  - 字符串方法，如 `split` / `replace` / `trim`
  - 结构化解析，如 JSON / HTML parser / schema
  - 最后才考虑 regex
- 避免内联复杂逻辑，尤其不要在 JSX 里堆业务判断。
- 复杂分支要提前拆成函数或 `useMemo` / `useCallback` 之类的明确结构。
- 注释要少，只在逻辑确实不自明时写简短注释。
- 命名要直接反映业务含义，不要用模糊缩写。

---

## 四、前端规范（React + TypeScript + Ant Design）

### 4.1 技术约束

- 使用函数组件 + Hooks。
- 避免 class component。
- 简单状态优先 `useState` / `useReducer`。
- 跨页面或共享状态优先沿用已有 `Zustand`，不要无故引入新的状态管理方案。
- UI 组件优先使用现有的 `Ant Design` 体系。

### 4.2 页面结构

- 页面职责要清晰：
  - 页面组件放在 `src/pages`
  - API 请求放在 `src/api`
  - 类型定义放在 `src/types`
  - 状态放在 `src/stores`
  - 通用工具放在 `src/utils`
- 新增特殊工作流时，优先新建独立页面，而不是继续往通用页面里堆条件分支。
- 组件文件避免无限膨胀；单文件超过 500 行必须模块化拆分，避免继续堆叠。

### 4.3 交互与视觉

- 优先保证页面一屏能看懂，不堆装饰，不堆说明。
- 不要把“给开发者解释实现思路”的文本写进用户页面。
- 列表、表格、弹窗、抽屉的选择要基于任务密度：
  - 高密度管理场景优先列表或表格
  - 需要聚焦单对象编辑时再用弹窗/独立页
- 预览图应服务于操作本身，不要喧宾夺主。
- 如果页面已经有明显主工作区，不要再塞重复的信息卡片或大段说明。

### 4.4 数据处理

- 不要在前端做复杂的业务字符串解析。
- 能在后端结构化返回的数据，不要在前端“猜格式”。
- 需要兼容历史数据时，兼容逻辑应集中在 `utils` 或清晰的解析函数里，不要散落在多个组件中。

### 4.5 验证要求

- 前端改动完成后，至少执行：
  - `npm run build`
- 如果是高风险页面改动，应额外检查：
  - 路由是否可达
  - 关键按钮是否可点击
  - 列表/分页/弹窗是否正常联动

---

## 五、后端规范（FastAPI）

### 5.1 结构约束

- 采用现有项目结构：
  - `router` 负责接口入口
  - `service` 负责业务逻辑
  - `schema` / `response model` 负责输入输出定义
- 不要把复杂业务逻辑直接写在路由函数里。
- 所有接口都应有明确的请求/响应 schema。

### 5.2 编码原则

- 业务逻辑应集中在 service 层。
- 路由层只做参数接收、调用 service、返回结果。
- 避免无边界的全局变量；确需全局常驻的对象，必须是明确的基础设施或模型管理对象。
- 错误处理要明确，不要吞异常。
- 分页、过滤、排序等接口约束要稳定，前后端保持一致。

### 5.3 数据与兼容性

- 数据格式一旦对外提供，就要尽量稳定。
- 兼容旧数据时，应通过显式兼容逻辑处理，而不是默默改变原有含义。
- 涉及标注、训练、部署等核心业务字段时，优先保持语义清晰，避免“字段还能顺带表示别的含义”。

---

## 六、微服务与通信规范

### 6.1 服务边界

- 当前项目是微服务架构，服务职责要明确：
  - `automl_server` 负责主业务入口与编排
  - `model_trainer` 负责训练
  - `model_deploy` 负责部署与推理服务实例管理
  - `auto_augment_pipeline` 负责 AI 辅助处理能力
- 不要把本该属于独立服务的职责重新耦合回主服务。

### 6.2 通信方式

- 服务间通信优先走 MQ，不要新增直接 HTTP 强耦合调用。
- 消息必须结构化，至少包含清晰的事件类型和 payload。
- 新增服务级能力时，优先考虑是否应走现有 MQ 通道，而不是临时拼 HTTP 调用。

### 6.3 运行时对象

- 模型、推理器、长生命周期资源应采用常驻复用策略。
- 禁止在请求路径中重复加载重量级对象。
- 应提供明确的获取、加载、释放边界。

---

## 七、配置与 Nacos 规范

- 配置中心主要用于业务配置，不要把会长期稳定不变的中间件配置设计得过于复杂。
- MySQL、MinIO、RabbitMQ 这类基础设施配置一般视为稳定配置，不需要围绕“频繁热更新”过度设计。
- 所有服务应具备统一的 Nacos 热更新能力框架，但热更新重点放在真正可能变化的业务配置上。
- 配置结构应尽量扁平、直接、可理解，不要为了所谓灵活性堆过多 profile / defaults / 多层 fallback。

---

## 八、标注、训练、部署相关业务约束

- 标注、训练、部署是三个独立阶段，不要把其中一个阶段的参数硬塞到另一个阶段。
- 训练配置属于训练任务。
- 推理配置属于部署后的调用阶段，不属于训练阶段，也不应和数据集配置强绑定。
- 数据集场景如果天然不同，应在数据集创建阶段就显式建模，而不是后续靠命名约定临时推断。
- 新标注类型或新业务工作流优先独立页面，不与已有标注页面强冲突。

---

## 九、改动策略

- 修改已有功能时，优先局部修正，不做无关重构。
- 如果发现现有页面/模块已不适合承载新场景，应拆分新页面，而不是继续打补丁。
- 非必要不要同时修改前端、后端、训练、部署多个系统。
- 如果必须跨服务改动，要先明确链路边界和数据格式，再动代码。

---

## 十、提交前自检清单

- 这个改动是否真的解决了用户当前问题？
- 是否引入了不必要的解释性 UI 文本？
- 是否破坏了现有页面或服务的职责边界？
- 是否为了抽象而抽象？
- 是否保留了旧数据或旧接口的必要兼容？
- 前端是否已执行 `npm run build`？
- 后端是否至少做了最小链路验证或静态检查？
